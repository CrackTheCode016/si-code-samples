# Symbol SI Code Samples

Here, you will find multiple code examples from the [Symbol docs](https://docs.symbolplatform.com/). These are the examples as shown in the SI training slides.

Installation:

```sh
cd si-code-samples/
npm install
```

Running the examples:

```sh
npm start
```
